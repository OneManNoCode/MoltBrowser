# Copyright 2023 The Chromium Authors
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# @generated from third_party/rust/chromium_crates_io/BUILD.gn.hbs by
# tools/crates/gnrt.
# Do not edit!

import("//build/rust/cargo_crate.gni")

cargo_crate("lib") {
  crate_name = "cfb"
  epoch = "0.14"
  crate_type = "rlib"
  crate_root =
      "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/lib.rs"
  sources = [
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/alloc.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/chain.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/color.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/consts.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/directory.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/direntry.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/entry.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/header.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/macros.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/minialloc.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/minichain.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/mod.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/objtype.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/path.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/sector.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/stream.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/stream_buffer.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/timestamp.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/validate.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/version.rs",
    "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/lib.rs",
  ]
  # MoltBrowser manual fix: cfb include!()s this char-map data file from src.
  # gnrt does not add non-.rs in-src include! files to `inputs`, so declare it
  # here. Re-apply if this BUILD.gn is ever regenerated by `gnrt gen`.
  inputs = [ "//third_party/rust/chromium_crates_io/vendor/cfb-v0_14/src/internal/uppercase.txt" ]

  build_native_rust_unit_tests = false
  edition = "2018"
  cargo_pkg_authors = "Matthew D. Steele <mdsteele@alum.mit.edu>"
  cargo_pkg_name = "cfb"
  cargo_pkg_description =
      "Read/write Compound File Binary (structured storage) files"
  cargo_pkg_repository = "https://github.com/mdsteele/rust-cfb"
  cargo_pkg_version = "0.14.0"

  allow_unsafe = false

  deps = [
    "//third_party/rust/fnv/v1:lib",
    "//third_party/rust/uuid/v1:lib",
    "//third_party/rust/web_time/v1:lib",
  ]

  # Only for usage from third-party crates. Add the crate to
  # //third_party/rust/chromium_crates_io/Cargo.toml to use
  # it from first-party code.
  visibility = [ "//third_party/rust/*" ]

  #####################################################################
  # Tweaking which GN `config`s apply to this target.

  # Config changes that apply to all `//third_party/rust` crates.
  _configs_to_remove = [
    # We don't need code coverage data for any `chromium_crates_io` crates.
    "//build/config/coverage:default_coverage",

    # This is third-party code, so remove `chromium_code` config.  We are not
    # at the same time adding `//build/config/compiler:no_chromium_code`,
    # because 1) we don't want to pull how warnings are handled by that config
    # and 2) that config doesn't have any non-warnings-related stuff.
    "//build/config/compiler:chromium_code",
  ]
  _configs_to_add = []

  # Changing (if needed) which configs apply to this specific crate (based on
  # `extra_kv.configs_to_remove` and `extra_kv.configs_to_add` from
  # `gnrt_config.toml`).
  _configs_to_remove += []
  _configs_to_add += []

  # Applying config changes.
  library_configs -= _configs_to_remove
  library_configs += _configs_to_add
  executable_configs -= _configs_to_remove
  executable_configs += _configs_to_add
  proc_macro_configs -= _configs_to_remove
  proc_macro_configs += _configs_to_add
}
